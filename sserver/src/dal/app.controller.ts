import { Get, Controller } from '@nestjs/common';
//import { UserAccountService } from './dal/user_account.service';

@Controller()
export class AppController {
/*
    @Get('b')
    root(): string {
        return 'Hello World!';
    }

    @Get('DBtest')
    DBtest(): string {
        const data = new UserService().getItem(0, 'user_account_id');
         return 'Hello DB!';
    }
*/
}
